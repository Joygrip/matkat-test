# Copy this file to .env and fill in values
# Environment
ENV=dev

# Dev bypass mode (NEVER enable in production)
DEV_AUTH_BYPASS=false

# Azure AD Configuration
# AZURE_TENANT_ID: Entra tenant GUID (e.g. xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx)
AZURE_TENANT_ID=
AZURE_TENANT_ALLOWLIST=
API_APP_CLIENT_ID=
API_APP_ID_URI=

# Database
DATABASE_URL=sqlite:///./dev.db

# Notifications
# NOTIFY_MODE: "stub" (log only, no emails sent) or "graph" (send via Microsoft Graph)
NOTIFY_MODE=stub
# NOTIFY_FROM_EMAIL: UPN of the shared mailbox used as the sender (e.g. notifications@company.com)
# Requires Mail.Send Application permission on the app registration (admin consent required).
NOTIFY_FROM_EMAIL=
# NOTIFY_CONFLICT_SCHEDULE: which phase cadence triggers resource conflict alert emails
NOTIFY_CONFLICT_SCHEDULE=PM_RO
# NOTIFY_MISSING_ACTUALS_SCHEDULE: which phase cadence triggers missing actuals reminder emails
NOTIFY_MISSING_ACTUALS_SCHEDULE=Employee

# Microsoft Graph (On-Behalf-Of sync — leave empty to disable Graph sync)
GRAPH_CLIENT_ID=
GRAPH_CLIENT_SECRET=

# Azure Application Insights (optional)
APPINSIGHTS_CONNECTION_STRING=
