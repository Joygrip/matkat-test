# Copy this file to .env.local and fill in values

# Azure AD Configuration
VITE_AUTH_CLIENT_ID=
VITE_AUTH_AUTHORITY=https://login.microsoftonline.com/common
VITE_API_SCOPE=

# API Configuration
VITE_API_BASE_URL=http://localhost:8000

# Dev bypass mode (NEVER enable in production)
VITE_DEV_AUTH_BYPASS=false

# Dev Sample Data Seeding is now handled automatically on backend startup
# No frontend configuration needed
